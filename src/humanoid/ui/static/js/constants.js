// Generated by `uv run python scripts/generate_js_constants.py`.
// Python enums and UI routes are the source of truth. Do not edit this file manually.

export const Runtime = Object.freeze({
  SIM: "sim",
  REAL: "real",
});

export const ProcessName = Object.freeze({
  STACK: "stack",
  REPLAY: "replay",
  KEYBOARD: "keyboard",
  OCULUS: "oculus",
});

export const ProcessAction = Object.freeze({
  START: "start",
  STOP: "stop",
});

export const Mode = Object.freeze({
  IDLE: "idle",
  HOMING: "homing",
  OCULUS: "oculus",
  KEYBOARD: "keyboard",
});

export const LoggingState = Object.freeze({
  STOPPED: "stopped",
  STARTING: "starting",
  RUNNING: "running",
  STOPPING: "stopping",
  FAILED: "failed",
});

export const ReplayOutcome = Object.freeze({
  COMPLETED: "completed",
  STOPPED: "stopped",
  FAILED: "failed",
});

export const OrchestratorParameter = Object.freeze({
  PRESET: "preset",
});

export const PayloadKey = Object.freeze({
  RUNTIME: "runtime",
  ROBOT: "robot",
  MODE: "mode",
  PARAMETERS: "parameters",
  EXPECTED_RUNTIME: "expected_runtime",
  EXPECTED_ROBOT: "expected_robot",
  REAL_HARDWARE_ACKNOWLEDGED: "real_hardware_acknowledged",
  RECORDING: "recording",
});

export const ApiRoute = Object.freeze({
  ROOT: "/api",
  STATUS: "/api/status",
  RUNTIME: "/api/runtime",
  ROBOT: "/api/robot",
  PROCESSES: "/api/processes/<name>/<action>",
  LOGGING: "/api/logging/<action>",
  REPLAY: "/api/replay/<action>",
  ORCHESTRATOR: "/api/orchestrator",
});

export const BusyKey = Object.freeze({
  RUNTIME: ApiRoute.RUNTIME,
  ROBOT: ApiRoute.ROBOT,
  LOGGING: ApiRoute.LOGGING,
  REPLAY: ApiRoute.REPLAY,
  ORCHESTRATOR: ApiRoute.ORCHESTRATOR,
});

export const TELEOP_PROCESSES = Object.freeze([
  ProcessName.KEYBOARD,
  ProcessName.OCULUS,
]);

export const API = Object.freeze({
  STATUS: ApiRoute.STATUS,
  RUNTIME: ApiRoute.RUNTIME,
  ROBOT: ApiRoute.ROBOT,
  logging(action) {
    return resolveRoute(ApiRoute.LOGGING, { action });
  },
  replay(action) {
    return resolveRoute(ApiRoute.REPLAY, { action });
  },
  ORCHESTRATOR: ApiRoute.ORCHESTRATOR,
  process(name, action) {
    return resolveRoute(ApiRoute.PROCESSES, { name, action });
  },
});

function resolveRoute(route, parameters) {
  return Object.entries(parameters).reduce(
    (result, [name, value]) =>
      result.replace(`<${name}>`, encodeURIComponent(value)),
    route,
  );
}
