#!/bin/bash

set -e

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${PROJECT_ROOT}"

uv run pytest \
    --cov=humanoid \
    --cov-report=term-missing \
    --cov-report=html \
    "$@"

echo ""
echo "HTML coverage report: ${PROJECT_ROOT}/htmlcov/index.html"
