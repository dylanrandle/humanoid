#!/bin/bash

set -euo pipefail

PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "${PROJECT_ROOT}"

uv run python scripts/generate_js_constants.py --check
node --test tests/ui/js/*.test.js
