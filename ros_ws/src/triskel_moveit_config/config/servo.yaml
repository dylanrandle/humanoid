publish_period: 0.02
max_expected_latency: 0.1
move_group_name: arm
planning_frame: base_link
ee_frame_name: gripper_base_link
robot_link_command_frame: base_link
command_in_type: speed_units
scale:
  linear: 0.05
  rotational: 0.3
  joint: 0.5
command_out_type: trajectory_msgs/JointTrajectory
publish_joint_positions: true
publish_joint_velocities: false
publish_joint_accelerations: false
use_smoothing: true
smoothing_filter_plugin_name: online_signal_smoothing::AccelerationLimitedPlugin
is_primary_planning_scene_monitor: false
check_octomap_collisions: false
cartesian_command_in_topic: ~/delta_twist_cmds
joint_command_in_topic: ~/delta_joint_cmds
joint_topic: /joint_states
status_topic: ~/status
command_out_topic: /arm_controller/joint_trajectory
check_collisions: true
collision_check_rate: 10.0
self_collision_proximity_threshold: 0.01
scene_collision_proximity_threshold: 0.02
# Triskel's calibrated home pose has a Jacobian condition number of about 41.
# Slow before that neighborhood and retain an emergency-stop margin beyond it.
# The folded rest pose is intentionally not a Cartesian teleoperation pose.
lower_singularity_threshold: 50.0
hard_stop_singularity_threshold: 80.0
leaving_singularity_threshold_multiplier: 2.0
joint_limit_margins: [0.08]
