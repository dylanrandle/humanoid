[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "humanoid"
version = "0.1.0"
description = "Small household humanoid robot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lcm>=1.5.2",
    "meshcat>=0.3.2",
    "numpy>=2.0.0",
    "oculus-reader",
    "pin>=3.2.0",
    "pin-pink>=4.0.0",
    "pynput>=1.8.1",
    "pyserial>=3.5",
    "qpsolvers[open-source-solvers]>=4.8.2",
    "vassar-feetech-servo-sdk>=1.5.0",
]

[project.scripts]
start = "humanoid.start:main"
visualizer = "humanoid.nodes.robot_visualizer:main"
orchestrator = "humanoid.nodes.orchestrator:main"
homing = "humanoid.nodes.policy.homing:main"
keyboard = "humanoid.nodes.policy.teleop.keyboard:main"
oculus = "humanoid.nodes.policy.teleop.oculus:main"

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
    "pytest>=8.0.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.15",
    "ty>=0.0.17",
]

[tool.ruff]
line-length = 100
target-version = "py313"
exclude = [
    "src/humanoid/types/lcm/*_t.py",  # Auto-generated LCM types
]

[tool.ruff.lint]
select = [
    "E",     # pycodestyle errors
    "W",     # pycodestyle warnings
    "F",     # pyflakes (includes unused imports)
    "I",     # isort (import sorting)
    "UP",    # pyupgrade
    "B",     # flake8-bugbear (common bugs and design problems)
    "C4",    # flake8-comprehensions (better list/dict/set comprehensions)
    "SIM",   # flake8-simplify (simplification suggestions)
    "PIE",   # flake8-pie (misc lints)
    "RET",   # flake8-return (return statement issues)
    "PTH",   # flake8-use-pathlib (prefer pathlib over os.path)
    "PL",    # pylint (comprehensive static analysis)
    "PERF",  # perflint (performance anti-patterns)
    "RUF",   # ruff-specific rules
]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["humanoid"]

[tool.ty.src]
exclude = [
    "src/humanoid/types/lcm/*_t.py",  # Auto-generated LCM types
]

[tool.ty.environment]
extra-paths = ["typings"]

[tool.hatch.build.targets.wheel]
packages = ["src/humanoid"]

[tool.uv.sources]
oculus-reader = { git = "https://github.com/rail-berkeley/oculus_reader.git", rev = "17bc7b3923f5754d70c4e358867a3bcac1a3c0c3" }
